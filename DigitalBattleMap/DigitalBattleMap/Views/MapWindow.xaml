<Window x:Class="DigitalBattleMap.Views.MapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DigitalBattleMap.Views"
        xmlns:viewmodels="clr-namespace:DigitalBattleMap.ViewModels"
        mc:Ignorable="d"
        Title="MapWindow" Height="1080" Width="1920"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        Background="{Binding BackgroundColor}"
        Left="{Binding Left, Mode=TwoWay}"
        WindowState="{Binding WindowState, Mode=TwoWay}">
    <Window.DataContext>
        <viewmodels:MapWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Image Source="{Binding BackgroundBitmapSource}" Margin="5"/>
        <Image Source="{Binding GridBitmapSource}" Margin="5"/>
        <Image Source="{Binding TokenBitmapSource}" Margin="5"/>
    </Grid>
</Window>
