<Window x:Class="DigitalBattleMap.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DigitalBattleMap.Views" xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="SettingsWindow" Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        FontSize="17">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Monitor position"/>
        <ComboBox Grid.Column="1" ItemsSource="{Binding MonitorPositions}" SelectedItem="{Binding SelectedMonitorPosition}" VerticalContentAlignment="Center"/>
        <Label Grid.Row="1" Content="Default grid size" Margin="0 10 0 0"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0 10 0 0" Text="{Binding DefaultGridSize, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
        <Label Grid.Row="2" Content="Server Address" Margin="0 10 0 0"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="0 10 0 0" Text="{Binding ServerAddress, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
        <Label Grid.Row="3" Content="Show map window" Margin="0 10 0 0"/>
        <CheckBox Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" Margin="0 10 0 0" IsChecked="{Binding ShowMapWindow}"/>
        <Label Grid.Row="4" Content="Hide DM features" Margin="0 10 0 0"/>
        <CheckBox Grid.Row="4" Grid.Column="1" VerticalAlignment="Center" Margin="0 10 0 0" IsChecked="{Binding HideDungeonMasterFeatures}"/>
        <Label Grid.Row="5" Content="Set black background" Margin="0 10 0 0"/>
        <CheckBox Grid.Row="5" Grid.Column="1" VerticalAlignment="Center" Margin="0 10 0 0" IsChecked="{Binding HasBlackBackground}"/>
        <Label Grid.Row="6" Content="Enable auto save" Margin="0 10 0 0"/>
        <CheckBox Grid.Row="6" Grid.Column="1" VerticalAlignment="Center" Margin="0 10 0 0" IsChecked="{Binding IsAutoSaveEnabled}"/>
        <Button Grid.Row="7" Grid.ColumnSpan="2" Content="Download monster tokens" Margin="0 10 0 0" Command="{Binding DownloadMonsterTokensCommand}"/>
        <Button Grid.Row="8" Grid.ColumnSpan="2" Content="Web extensions" Margin="0 10 0 0" Command="{Binding WebExtensionsCommand}"/>

        <Button Grid.Row="9" Content="Import" Margin="0 10 5 0" Command="{Binding ImportCommand}"/>
        <Button Grid.Row="9" Grid.Column="1" Content="Export" Margin="5 10 0 0" Command="{Binding ExportCommand}"/>

        <Button Grid.Row="10" Grid.ColumnSpan="2" Content="Save" Command="{Binding SaveCommand}" IsDefault="True" Margin="0 10 0 0">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                    <i:CallMethodAction MethodName="Close" TargetObject="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Button>
    </Grid>
</Window>
